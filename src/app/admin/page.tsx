export default function AdminDashboard() {
  return (
    <main className="p-8">
      <h1 className="text-2xl font-semibold">Admin</h1>
      <p className="mt-2 text-muted-foreground">
        Product, collection, and homepage curation land here — see docs/ADMIN.md.
      </p>
    </main>
  );
}
